#include "monty.h"
/**
 * _stack - sets the format of the data to a stack (LIFO)
 * @stack: head
 * @num_line: num line
 */

void _stack(stack_t **stack, unsigned int num_line)
{
	if (stack || num_line)
		global.flag = 1;
}
