#include "monty.h"
/**
 * _queue - sets the format of the data to a queue (FIFO)
 * @stack: head
 * @num_line: num line
 */

void _queue(stack_t **stack, unsigned int num_line)
{
	if (stack || num_line)
		global.flag = 0;
}
