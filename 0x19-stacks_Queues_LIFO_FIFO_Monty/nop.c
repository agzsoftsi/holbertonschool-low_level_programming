#include "monty.h"

/**
 * _nop - doesn’t do anything.
 * @stack: head
 * @num_line: number of the line
 */
void _nop(stack_t **stack, unsigned int num_line)
{
	if (*stack)
	{};
	if (num_line)
	{};
}
